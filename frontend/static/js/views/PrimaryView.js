export default class {
    constructor(parameters) {
        this.parameters = parameters;
    }

    setTitle(title) {
        document.title = title;
    }

    async getHtml() {
        return "";
    }
}